name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  preflight:
    uses: ./.github/workflows/release.yml
    permissions:
      contents: write
      id-token: write
