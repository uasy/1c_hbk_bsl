# BSL276 — <span class="doc-lang doc-lang-ru">Некорректное использование функции ПродолжитьВызов()</span><span class="doc-lang doc-lang-en">Wrong use of ProceedWithCall function</span>

<!-- generated-rule-header:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

[← Все правила](../diagnostic-rules.md)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

[← All rules](../diagnostic-rules.md)

</div>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Некорректное использование функции ПродолжитьВызов()

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Wrong use of ProceedWithCall function

</div>

<div class="doc-lang doc-lang-ru" markdown="1">

## Идентификаторы

| Поле | Значение |
|---|---|
| Код правила | `BSL276` |
| Совместимый псевдоним | `WrongUseFunctionProceedWithCall` |
| Серьёзность | `ERROR` |
| Включено по умолчанию | Да |
| Реализовано | Да |
| Теги | `correctness`, `extensions` |

## Поведение

- Публичный идентификатор `BSL276` и псевдоним `WrongUseFunctionProceedWithCall` стабильны.
- Правило сообщает о случаях, описанных на этой странице.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL276"]
ignore = ["WrongUseFunctionProceedWithCall"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL276
Значение = "пример";  // bsl-disable: BSL276
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:WrongUseFunctionProceedWithCall-off
// код без этой диагностики
// BSLLS:WrongUseFunctionProceedWithCall-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Identifiers

| Field | Value |
|---|---|
| Rule code | `BSL276` |
| Compatible alias | `WrongUseFunctionProceedWithCall` |
| Severity | `ERROR` |
| Enabled by default | Yes |
| Implemented | Yes |
| Tags | `correctness`, `extensions` |

## Behavior

- The public identifier `BSL276` and alias `WrongUseFunctionProceedWithCall` are stable.
- The rule reports the cases documented on this page.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL276"]
ignore = ["WrongUseFunctionProceedWithCall"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL276
Value = "example";  // bsl-disable: BSL276
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:WrongUseFunctionProceedWithCall-off
// code without this diagnostic
// BSLLS:WrongUseFunctionProceedWithCall-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Использование функции `ПродолжитьВызов` не в методах расширения с аннотацией `&Вместо` приведет к ошибке времени выполнения.

## Примеры

```bsl
&НаКлиенте
Процедура Тест()

    // этот код возможно был внесен копипастой, при переносе из расширения
    ПродолжитьВызов(); // Срабатывание здесь

КонецПроцедуры
```

## Источники

Источник: [Расширения конфигураций. Функциональность -> Модули](https://its.1c.ru/db/pubextensions#content:54:1)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Using the `ProceedWithCall` function outside of extension methods with the `&Around` annotation will result in a run-time error.

## Examples

```bsl
&AtClient
Procedure Test()

    // copy-past from extension
    ProceedWithCall(); // Срабатывание здесь

EndProcedure
```

## Sources

Source: [Extensions. Functionality -> Modules (RU)](https://its.1c.ru/db/pubextensions#content:54:1)

</div>

<!-- localized-rule-description:end -->
